# ==========================================
# Cloudflare + Supabase 架构环境变量配置
# ==========================================

# Supabase 配置
# 访问 https://app.supabase.com/project/_/settings/api 获取
# 选择 FREE 免费计划即可（500MB 数据库 + 1GB 存储）
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-public-key-here
SUPABASE_SERVICE_KEY=your-service-role-key-here

# NextAuth 配置
NEXTAUTH_URL="http://localhost:3000"
NEXTAUTH_SECRET="your-secret-key-min-32-chars-please-change-this"

# 应用配置（Cloudflare 部署用）
BUILD_TARGET=cloudflare
NODE_ENV=development
NEXT_PUBLIC_SITE_URL=http://localhost:3000

# ==========================================
# 第三方 API 配置（保持不变）
# ==========================================

# USDA API (免费注册: https://fdc.nal.usda.gov/api-guide.html)
USDA_API_KEY="your-usda-api-key"

# OpenAI API (用于 AI 营养建议)
OPENAI_API_KEY="your-openai-api-key"

# OpenRouter API (用于 Claude 集成)
OPENROUTER_API_KEY="your-openrouter-key"

# ==========================================
# 可选配置
# ==========================================

# Redis (可选，用于缓存)
# REDIS_URL="your-upstash-redis-url"

# Google OAuth (可选，用于社交登录)
# GOOGLE_CLIENT_ID="your-google-client-id"
# GOOGLE_CLIENT_SECRET="your-google-client-secret"
