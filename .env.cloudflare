# Cloudflare Pages + Supabase 混合架构环境变量
# 复制此文件为 .env.local 并填入实际值

# Supabase 配置
NEXT_PUBLIC_SUPABASE_URL=https://your-project.supabase.co
NEXT_PUBLIC_SUPABASE_ANON_KEY=your-anon-key
SUPABASE_SERVICE_KEY=your-service-key

# 站点配置
NEXT_PUBLIC_SITE_URL=https://hearthbulter-supabase.pages.dev

# 认证配置
NEXTAUTH_SECRET=your-nextauth-secret-at-least-32-characters-long
NEXTAUTH_URL=https://hearthbulter-supabase.pages.dev

# 第三方 API 配置
OPENAI_API_KEY=your-openai-api-key
OPENROUTER_API_KEY=your-openrouter-api-key
USDA_API_KEY=your-usda-api-key

# 可选：Redis 缓存（如果需要）
# UPSTASH_REDIS_REST_URL=your-upstash-url
# UPSTASH_REDIS_REST_TOKEN=your-upstash-token

# 可选：邮件服务
# SMTP_HOST=smtp.gmail.com
# SMTP_PORT=587
# SMTP_USER=your-email@gmail.com
# SMTP_PASS=your-app-password

# 可选：文件存储
# CLOUDFLARE_R2_BUCKET=your-r2-bucket
# CLOUDFLARE_R2_ACCESS_KEY=your-r2-access-key
# CLOUDFLARE_R2_SECRET_KEY=your-r2-secret-key

# 开发环境配置
NODE_ENV=development
