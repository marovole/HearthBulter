# Cloudflare Workers 轻量级配置文件
# 针对3MB限制优化

name = "health-butler-light"
main = ".open-next/worker-light.mjs"
compatibility_date = "2024-09-23"
compatibility_flags = ["nodejs_compat"]

# 环境变量
[vars]
NODE_ENV = "production"
APP_NAME = "Health Butler Light"
VERSION = "1.0.0"
